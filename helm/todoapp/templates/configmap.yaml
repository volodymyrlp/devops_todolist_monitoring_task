apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todoapp.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todoapp.labels" . | nindent 4 }}
data:
  PYTHONUNBUFFERED: "1"
